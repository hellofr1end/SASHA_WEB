// Задача 4: Кошелек (логика с проверкой)

// Создай объект wallet. У него есть balance (рубли) и методы:

// showBalance() — возвращает баланс.

// addMoney(amount) — увеличивает баланс (но если amount отрицательное — не делай ничего и верни "Ошибка").

// buy(price) — если денег хватает, вычитает и возвращает "Куплено", если нет — "Не хватает".


// Проверка: Попробуй купить что-то дорогое.


// Код проверки

console.log(wallet.buy(150)); // "Не хватает"
wallet.addMoney(200);
console.log(wallet.buy(150)); // "Куплено"
console.log(wallet.showBalance()); // "150 руб"
